deployments:
  Magento:
    filename:
      ./docker-compose.yml
    auto_deploy_on_commits:
      true
    post_launch:
      - target: web
        command: exec
        options: "{target} sh -c 'install-magento'"
